function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
function setup(){
  //you must include setup function to run code
  //only run this code block once per sketch

  //set size of our canvas area with width and height

  createcanvas(800, 500);

  //change background color with RGB values 0-255
  background(110);
}